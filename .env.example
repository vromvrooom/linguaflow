# Chrome extension ID for LinguaFlow, used by the webapp to push auth to the
# extension (SET_AUTH). Find it at chrome://extensions (enable Developer mode).
# Consumed at build time by webapp (NEXT_PUBLIC_* vars are inlined during build).
NEXT_PUBLIC_EXTENSION_ID=
